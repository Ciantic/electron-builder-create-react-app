{
    "name": "electron-builder-create-react-app",
    "version": "1.0.0",
    "main": "index.js",
    "author": "",
    "license": "MIT",
    "private": true,
    "workspaces": [
        "host-electron",
        "service",
        "gui"
    ],
    "devDependencies": {
        "concurrently": "^4.1.0"
    },
    "scripts": {
        "start": "concurrently --kill-others -n gui,service,electron \"cd gui && yarn start\" \"cd service && yarn start\" \"cd host-electron && yarn start\"",
        "build": "yarn --cwd gui build && yarn --cwd service build && yarn --cwd host-electron build"
    }
}
